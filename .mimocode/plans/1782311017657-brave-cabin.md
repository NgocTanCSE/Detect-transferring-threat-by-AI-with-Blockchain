# Fix: Remove all login/register references from frontend

## Problem
HF build fails because files still reference deleted login/register pages.

## Files to fix

### 1. `frontend/src/components/dashboard-utils.tsx` (line 279-284)
Remove Login and Register from QUICK_ROUTES:
```
export const QUICK_ROUTES = [
  { label: "User Exchange", href: "/user/exchange" },
  { label: "User History", href: "/user/history" },
];
```

### 2. `frontend/src/components/live-dashboard.tsx` (line 161)
Remove login/register filter logic.

## Verification
1. Push to GitHub
2. HF rebuild should pass
3. No more login/register references in codebase
