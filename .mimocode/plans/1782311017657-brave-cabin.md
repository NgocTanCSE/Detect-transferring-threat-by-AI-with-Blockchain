# Plan: Simplify Auth - Auto-login Test User, Remove Admin Auth

## Goal
Remove all login/register complexity. Auto-login as a test user for coin transfer testing.

## Changes

### 1. Frontend Auth Context - Auto-login
**File: `frontend/src/lib/auth-context.tsx`**
- On mount, if no token exists, auto-create a fake token and set user as `{username: "testuser", role: "user", wallet_address: "0x742d35Cc6634C0532925a3b844Bc454e4438f44e"}`
- No need to call backend `/auth/login` or `/auth/me`
- Keep token in localStorage for API calls

### 2. Frontend Middleware - Remove All Auth Checks
**File: `frontend/src/middleware.ts`**
- Remove ADMIN_ROUTES, ANALYST_ROUTES, USER_ROUTES arrays
- Remove all redirect logic
- Simply `return NextResponse.next()` for all routes

### 3. Delete Login/Register Pages
**Delete:**
- `frontend/src/app/login/page.tsx`
- `frontend/src/app/register/page.tsx`

### 4. Remove Root Page Redirect
**File: `frontend/src/app/user/page.tsx`**
- Remove `router.replace("/login")` redirect
- Just show a simple landing or redirect to `/user/dashboard`

### 5. Backend - Keep Auth Endpoints But Make Optional
**File: `backend/app/auth.py`**
- Keep endpoints (don't break anything)
- But `optional_auth` already works - endpoints that use it will function without token

### 6. Seed Default Test User
**File: `backend/seed_wallets.py`**
- Ensure user `testuser` with password `test123` exists
- Wallet address: `0x742d35Cc6634C0532925a3b844Bc454e4438f44e`

### 7. Remove Logout Button from Exchange Page
**File: `frontend/src/app/user/exchange/page.tsx`**
- Remove logout button and auth redirect check

## Files to Modify
1. `frontend/src/lib/auth-context.tsx` - Auto-login
2. `frontend/src/middleware.ts` - Remove auth checks
3. `frontend/src/app/login/page.tsx` - DELETE
4. `frontend/src/app/register/page.tsx` - DELETE
5. `frontend/src/app/user/page.tsx` - Remove login redirect
6. `frontend/src/app/user/exchange/page.tsx` - Remove logout/auth
7. `backend/seed_wallets.py` - Ensure testuser exists

## Verification
1. Push to GitHub → HF rebuild
2. Access HF URL directly → should go to user dashboard
3. No login form shown
4. Exchange page works for testing transfers
5. All user pages accessible without login
